<header class="main-header">
    <a href="@Url.Action("Index","Home")" class="logo">
        <span class="logo-mini"><b>V</b>ST</span>
        <span class="logo-lg"><b>Visit</b>ation</span>
    </a>
    <nav class="navbar navbar-static-top">
        <a href="#" class="sidebar-toggle" data-toggle="push-menu" role="button">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </a>
        <div class="navbar-custom-menu">
            <ul class="nav navbar-nav">
                <li class="dropdown user user-menu">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <span class="hidden-xs">@User.Claims.First(x => x.Type == System.Security.Claims.ClaimTypes.Name).Value @User.Claims.First(x => x.Type == System.Security.Claims.ClaimTypes.Surname).Value</span>
                    </a>
                    <ul class="dropdown-menu">
                        <li class="user-footer">
                            <a href="@Url.Action("Logout","Auth")" class="btn btn-default btn-flat">Çıkış Yap</a>
                        </li>
                    </ul>
                </li>

            </ul>
        </div>
    </nav>
</header>